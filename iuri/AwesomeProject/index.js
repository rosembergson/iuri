/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Navigation from './navegacao/index';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Navigation);
